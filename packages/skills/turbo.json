{
  "extends": ["//"],
  "tasks": {
    "update": {
      "dependsOn": ["@reaxuse/metadata#build"],
      "outputs": [
        "skills/**",
        "$TURBO_ROOT$/skills/reaxuse-functions/**"
      ]
    }
  }
}

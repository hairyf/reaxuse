/**
 * @reaxuse/router — React port of @vueuse/router
 *
 * Router bindings. NOTE: the router adapter (e.g. react-router) is still an
 * open decision — keep the router dependency optional and external until the
 * mapping issues settle it.
 */

// export * from './useRouteHash'
// export * from './useRouteParams'
// export * from './useRouteQuery'
export {}

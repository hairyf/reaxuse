/**
 * @reaxuse/metadata — React port of @vueuse/metadata
 *
 * TODO: collect metadata of every mapped function (category, description,
 * packages, related functions) to drive the docs site and the mapping
 * progress table — see ../../meta/functions.md
 */
export {}

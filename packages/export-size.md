# Export size

version: 0.1.4<br>
measured: 2026-09-12<br>
generated by [`npm run size`](https://github.com/hairyf/reause/blob/main/scripts/export-size.ts) (run `npm run build` first)

> Each figure is the gzip size of a **built entry file** (`min+gzipped`), not a tree-shaken per-export bundle: importing two functions does not mean their entries sum, and the real result depends on your bundler and minifier — treat this list as a reference only.

Per-package ESM bundles (`dist/index.js`, min+gzipped):

| Package                | min+gzipped |
| ---------------------- | ----------- |
| `@reause/core`         | 174.10 kB   |
| `@reause/shared`       | 31.16 kB    |
| `@reause/metadata`     | 15.48 kB    |
| `@reause/rxjs`         | 5.85 kB     |
| `@reause/math`         | 4.08 kB     |
| `@reause/electron`     | 2.34 kB     |
| `@reause/integrations` | 0.28 kB     |
| `@reause/firebase`     | 0.10 kB     |

`@reause/integrations` and `@reause/firebase` publish one entry per hook, so their barrels only re-export; the per-hook entries are the sizes that matter:

<kbd>@reause/integrations</kbd>

| Function            | min+gzipped |
| ------------------- | ----------- |
| `useDrauu`          | 3.24 kB     |
| `useAxios`          | 3.06 kB     |
| `useSortable`       | 2.53 kB     |
| `useIDBKeyval`      | 2.49 kB     |
| `useAsyncValidator` | 2.13 kB     |
| `useNProgress`      | 2.02 kB     |
| `useFocusTrap`      | 1.85 kB     |
| `useCookies`        | 1.81 kB     |
| `useFuse`           | 1.40 kB     |
| `useChangeCase`     | 1.28 kB     |
| `useQRCode`         | 1.21 kB     |
| `useJwt`            | 1.03 kB     |

<kbd>@reause/firebase</kbd>

| Function       | min+gzipped |
| -------------- | ----------- |
| `useFirestore` | 1.87 kB     |
| `useAuth`      | 1.63 kB     |
| `useRTDB`      | 1.10 kB     |

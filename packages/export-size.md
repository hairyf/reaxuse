# Export size

version: 0.1.2<br>
measured: 2026-09-12<br>
generated by [`npm run size`](https://github.com/hairyf/reaxuse/blob/main/scripts/export-size.ts) (run `npm run build` first)

Per-package ESM bundles (`dist/index.js`, min+gzipped):

| Package                 | min+gzipped |
| ----------------------- | ----------- |
| `@reaxuse/core`         | 168.13 kB   |
| `@reaxuse/shared`       | 30.43 kB    |
| `@reaxuse/metadata`     | 16.04 kB    |
| `@reaxuse/rxjs`         | 5.71 kB     |
| `@reaxuse/math`         | 3.99 kB     |
| `@reaxuse/electron`     | 2.29 kB     |
| `@reaxuse/integrations` | 0.27 kB     |
| `@reaxuse/firebase`     | 0.09 kB     |

`@reaxuse/integrations` and `@reaxuse/firebase` publish one entry per hook, so their
barrels only re-export; the per-hook entries are the sizes that matter:

<kbd>@reaxuse/integrations</kbd>

| Function            | min+gzipped |
| ------------------- | ----------- |
| `useDrauu`          | 3.17 kB     |
| `useAxios`          | 2.99 kB     |
| `useSortable`       | 2.47 kB     |
| `useIDBKeyval`      | 2.43 kB     |
| `useAsyncValidator` | 2.08 kB     |
| `useNProgress`      | 1.98 kB     |
| `useFocusTrap`      | 1.81 kB     |
| `useCookies`        | 1.77 kB     |
| `useFuse`           | 1.37 kB     |
| `useChangeCase`     | 1.25 kB     |
| `useQRCode`         | 1.18 kB     |
| `useJwt`            | 1.01 kB     |

<kbd>@reaxuse/firebase</kbd>

| Function       | min+gzipped |
| -------------- | ----------- |
| `useFirestore` | 1.82 kB     |
| `useAuth`      | 1.59 kB     |
| `useRTDB`      | 1.08 kB     |

> Please note this is bundle size for each individual API. Since we have a lot
> of shared utilities underneath each function, importing two different
> functions does NOT necessarily mean the bundle size will be the sum of them
> (usually smaller). Depends on the bundler and minifier you use, the final
> result might vary, this list is for reference only.

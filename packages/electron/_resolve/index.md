---
category: '@Electron'
---

# resolveIpcRenderer / resolveWebFrame

Internal helpers shared by the `@reaxuse/electron` hooks. They are not part of the package's public API — `packages/electron/src/index.ts` does not export them — but every module under `packages/electron/src/` has a page on this site, so they are documented here.
